'use client'

// Re-export the canonical shadcn hook (no local state, no aliases, no star export).
export { useToast, toast } from '@/components/ui/use-toast'

// (Optional) re-export types you might need elsewhere.
export type { Toast } from '@/components/ui/use-toast'
export type { ToastActionElement, ToastProps } from '@/components/ui/toast'
